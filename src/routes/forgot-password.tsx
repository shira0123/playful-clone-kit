import { createFileRoute, Link } from "@tanstack/react-router";
import { type FormEvent, useState } from "react";
import { AuthFrame, FormMessage } from "@/components/auth/AuthFrame";
import { forgotPassword } from "@/server-fns";
export const Route = createFileRoute("/forgot-password")({ component: ForgotPassword });
function ForgotPassword() { const [message, setMessage] = useState<string>(); const [error, setError] = useState<string>(); async function submit(event: FormEvent<HTMLFormElement>) { event.preventDefault(); setError(undefined); try { await forgotPassword({ data: { email: String(new FormData(event.currentTarget).get("email")) } }); setMessage("If an active account matches that email, a reset link is on its way."); } catch (cause) { setError(cause instanceof Error ? cause.message : "Unable to send reset email."); } } return <AuthFrame title="Reset your password" subtitle="Enter your email and we'll send a secure reset link."><form className="mt-7 space-y-4" onSubmit={submit}><label className="block text-sm font-medium">Email<input required type="email" name="email" className="mt-1 w-full rounded-md border px-3 py-2" /></label><button className="w-full rounded-md bg-gold px-4 py-2.5 font-semibold text-navy">Send reset link</button></form><FormMessage message={error} />{message && <p className="mt-4 rounded-md bg-green-50 px-3 py-2 text-sm text-green-800">{message}</p>}<p className="mt-5 text-center text-sm"><Link to="/login" className="text-navy underline">Back to sign in</Link></p></AuthFrame>; }
